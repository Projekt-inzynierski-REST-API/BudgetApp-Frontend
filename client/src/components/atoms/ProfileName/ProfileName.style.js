import styled from "styled-components";

const StyledProfileName = styled.div`
  height: 4vh;
  padding-right: 1vh;
  font-size: medium;
  font-weight: 600;
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
`;

const StyledEmailName = styled.div`
  font-size: small;
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  display: flex;
  justify-content: center;
`;

const StyledNamingSection = styled.div``;

export { StyledProfileName, StyledEmailName, StyledNamingSection };
